import { UserRoleDisplayPipe } from './user-role-display.pipe';

describe('UserRoleDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new UserRoleDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
